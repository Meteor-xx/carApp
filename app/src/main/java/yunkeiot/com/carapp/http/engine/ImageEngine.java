package yunkeiot.com.carapp.http.engine;

import android.content.Context;
import android.widget.ImageView;

public class ImageEngine {
    /**
     * 加载图片并且显示
     *
     * @param context
     * @param destImageView
     * @param url
     */
    public static void display(Context context, ImageView destImageView, String url) {
//        GlideApp.with(context).load(url).into(destImageView);
    }
}
